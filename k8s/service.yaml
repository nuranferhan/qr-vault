apiVersion: v1
kind: Service
metadata:
  name: qr-vault
  namespace: default
  labels:
    app: qr-vault
spec:
  type: NodePort
  selector:
    app: qr-vault
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
